# Agent Guide

目標：維持「零相依、純靜態」(HTML/CSS/JS) 的 Hello World 網頁，能直接用 GitHub Pages 部署。

## 技術限制
- 不要新增建置流程與套件相依（npm/pip 等）。
- 只修改 `index.html` / `styles.css` / `app.js` 這類靜態檔。
- 優先使用原生 Web API 與少量 JS。

## 驗收
- 打開 Pages 連結可看到 Hello World。
- 點擊按鈕可更新畫面文字（簡單互動）。
- 行動裝置寬度下版面仍可讀。
